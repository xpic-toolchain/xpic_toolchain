all tools inside this directory are required for running the test suites under windows 7 64bit

These tools will be packed together with the xpic-compiler

Make sure to update the tools inside this location, because the global build script uses
this directory as sources for packaging
